Documentation
Explain Sync for Didj
    fsync doesn't seem to work on windows
        but eject will wait till files are synced
    fsync works on linux
        but eject happens regardless if files are synced or not

    recommened enabling it for both, just incase. especially linux.

Make firmware folder easier to deal with
	search for files instead of folder
	extract if needed
	Lpad in folders or not


config file
should hold file/<name> info
list of nicknames and internal name/folder name of device

